<div id="tasklist-<%= task_list.id %>" class="panel panel-default">
  <div class="panel-heading">
      <div class="row">
        <div class="col-xs-8">
          <h3 class="panel-title"><%= task_list.title %></h3>
        </div>
        <div class="col-xs-4">
          <div class="text-right">(全<%= task_list.todos.count %>件中<%= task_list.todos.search_done.count %>件完了)</div>
        </div>
      </div>
  </div>
  <div class="panel-body">
    <% if task_list.todos.any? %>
      <p>もっとも早いToDoは<%= task_list.todos.fast_todo.item %>です。(<%= task_list.todos.fast_todo.todo_limit %>まで)</p>
    <% else %>
      <p>登録されているTodoはありません。</p>
    <% end %>
    <p>作成日時: <time><%= task_list.created_at %></time></p>
  </div>
  <div class="panel-footer">
    <div class="row">
      <div class="col-xs-8">
        <%= link_to "詳細を見る", task_list_todos_path(task_list.id), class: "btn btn-block btn-task" %>
      </div>
      <div class="col-xs-4">
        <%= link_to "削除する", task_list_path(task_list.id), method: :delete, class: "btn btn-block btn-danger", data: { confirm: "削除していいですか？"} %>
      </div>
    </div>
  </div>
</div>
