<div id="tasklist-<%= task_list.id %>" class="panel panel-default">
  <div class="panel-heading">
      <div class="row">
        <div class="col-xs-6">
          <h3 class="panel-title"><%= task_list.title %>のファイル</h3>
        </div>
        <div class="col-xs-6">
          <div class="pull-right">(全<%= task_list.todos.count %>件中<%= task_list.todos.search_done.count %>件完了)</div>
        </div>
      </div>
  </div>
  <div class="panel-body">
    <% if task_list.todos.any? && task_list.todos.search_before_work.first.present? %>
      <p>ファイルの中で一番早いやることは、<%= task_list.todos.search_before_work.first.item %>です。(<%= task_list.todos.search_before_work.first.todo_limit %>まで)</p>
    <% else %>
      <p>表示できるやることはありません。</p>
    <% end %>
    <p>作成日時: <time><%= task_list.created_at %></time></p>

  </div>
  <div class="panel-footer">
    <div class="row">
      <div class="col-xs-4">
        <%= link_to "見る", task_list_todos_path(task_list.id), class: "btn btn-block btn-task" %>
      </div>
      <div class="col-xs-4">
        <%= link_to "編集", edit_task_list_path(task_list.id), class: "btn btn-block btn-edit", remote: true %>
      </div>
      <div class="col-xs-4">
        <%= link_to "削除する",
            task_list_path(task_list.id), method: :delete, class: "btn btn-block btn-delete",
              data: { confirm: "登録済みのやることすべて削除されます。よろしいですか？"} %>
      </div>
    </div>
  </div>
</div>
